---
- name: Prepare
  hosts: all
  become: true
  tasks:
    - name: Install python3-debian for deb822_repository module
      ansible.builtin.apt:
        update_cache: true
        name:
          - python3-debian
        state: present
